# Stacks detected: cicd:github-actions:.github/workflows,deps:python:.,file:setup.py:.
version: 2.1
orbs:
  python: circleci/python@2

commands:
  security_scans:
    steps:
      - run:
          name: "SonarQube: Install and Execute Analysis"
          command: |
            echo "Installing SonarScanner dependencies..."
            # Install OpenJDK and Unzip
            sudo apt-get update && sudo apt-get install -y openjdk-17-jre unzip
            
            echo "Downloading SonarScanner..."
            export SONAR_SCANNER_VERSION=5.0.1.3006
            export SONAR_SCANNER_HOME=$HOME/.sonar/sonar-scanner-$SONAR_SCANNER_VERSION-linux
            curl --create-dirs -sSLo $HOME/.sonar/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-$SONAR_SCANNER_VERSION-linux.zip
            unzip -qq $HOME/.sonar/sonar-scanner.zip -d $HOME/.sonar/
            export PATH=$SONAR_SCANNER_HOME/bin:$PATH
            
            echo "Running SonarQube Analysis..."
            # Execute SonarQube scan
            # NOTE: Update the projectKey below to match your project
            sonar-scanner \
              -Dsonar.projectKey=rococo \
              -Dsonar.sources=. \
              -Dsonar.host.url=$SONAR_GLOBAL_HOST \
              -Dsonar.token=$SONAR_GLOBAL_TOKEN \
              -Dsonar.scm.disabled=true \
              -Dsonar.coverage.exclusions=tests/**,tests/**/*.py \
              -Dsonar.python.coverage.reportPaths=coverage.xml \
              -Dsonar.python.version=3.10 \
              -Dsonar.exclusions=**/.coverage,**/.coverage.*,**/htmlcov/**,**/.scannerwork/**,**/__pycache__/**,**/*.pyc
            
            echo "✅ SonarQube scan completed successfully"

jobs:
  test-python:
    # Install dependencies and run tests
    docker:
      - image: cimg/python:3.10-node
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
      - python/install-packages:
          pip-dependency-file: requirements.txt
          pkg-manager: pip
      - python/install-packages:
          args: pytest pytest-mock
          pkg-manager: pip
          pypi-cache: false
      - run:
          name: Run tests
          command: pytest --junitxml=junit.xml || ((($? == 5)) && echo 'Did not find any tests to run.')
      - store_test_results:
          path: junit.xml

  sonarqube-scan:
    # Run SonarQube analysis on main branch only
    docker:
      - image: cimg/python:3.10-node
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
      - python/install-packages:
          pip-dependency-file: requirements.txt
          pkg-manager: pip
      - python/install-packages:
          args: pytest pytest-cov pytest-mock
          pkg-manager: pip
          pypi-cache: false
      - run:
          name: Run tests with coverage for SonarQube
          command: pytest --cov=rococo --cov-report=xml:coverage.xml --cov-report=term --junitxml=junit.xml || ((($? == 5)) && echo 'Did not find any tests to run.')
      - security_scans

  build-package:
    # build python package
    docker:
      - image: cimg/python:3.10-node
    steps:
      - checkout
      - run:
          name: Create the ~/artifacts directory if it doesn't exist
          command: mkdir -p ~/artifacts
      - python/dist
      - store_artifacts:
          path: dist
          destination: ~/artifacts

  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
      - run:
          name: found github actions config
          command: ':'

workflows:
  build-and-test:
    jobs:
      - test-python
      - sonarqube-scan:
          context:
            - sonarqube
          filters:
            branches:
              only:
                - main
                - ilyaskarim-roc-93
      - build-package:
          requires:
            - test-python
    # - deploy:
    #     requires:
    #       - build-package